# Keys_Italian_Keyboard_programmer
A keyboard layout for italian keyboard. It has tilde `~` (ASCII 126) under `ALTGR + ì` and grave accent ` (ASCII 96) under `ALTGR + '`
Dead keys under `ALTGR + <` and `ALTGR + e`.
